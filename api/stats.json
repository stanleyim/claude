import fs from "fs";

export default function handler(req, res) {
  const file = fs.readFileSync("./data/stats.json", "utf8");
  res.setHeader("Access-Control-Allow-Origin", "*");
  res.status(200).json(JSON.parse(file));
}
